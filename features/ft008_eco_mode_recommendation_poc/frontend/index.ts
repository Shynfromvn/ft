/**
 * Điểm vào UI của gói POC. `generate_frontend_registry.py` sinh ra một dynamic
 * import trỏ vào `@features/ft008_eco_mode_recommendation_poc/frontend`, nên
 * export mặc định ở đây là thứ nền tảng mount.
 */
export { default } from "./DetailScreen";