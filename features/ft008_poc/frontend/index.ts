/**
 * Điểm vào UI của FT-007. `generate_frontend_registry.py` sinh ra một dynamic
 * import trỏ vào `@features/ft007_battery_status_recommendation/frontend`, nên
 * export mặc định ở đây là thứ nền tảng mount.
 */
export { default } from "./DetailScreen";
